plugins {
    id 'java'
}

apply from: 'version.gradle'

version = '0.0.1-SNAPSHOT'

allprojects {
    group = 'ru.kirill'
    repositories {
        mavenCentral()
    }
}

test {
    useJUnitPlatform()
}