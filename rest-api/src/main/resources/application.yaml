server:
  port: 0
  error:
    include-message: always

spring:
  application:
    name: rest-api
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: postgresql
  jpa:
    properties:
      hibernate:
        show_sql: true